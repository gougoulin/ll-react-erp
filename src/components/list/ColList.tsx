import React, { ReactNode } from "react";
import styled from "styled-components";

const Container = styled.ul`
  display: flex;
  flex-flow: column nowrap;
`;

const ColList = Container;

export default ColList;
