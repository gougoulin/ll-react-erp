import styled from 'styled-components';

const StyledCenterAllBox = styled.div`
  display: flex;
  flex-flow: row wrap;
  place-content: center;
`

const CenterAllBox = StyledCenterAllBox;

export default CenterAllBox